//Write a function that takes a string and returns an total representing the character count for each letter of each string in the sentence. Use .reduce to build this object. Hint: See .split()`` and string length``` property.

function main() {

    var str = "WHATTHEHELLISGOINGON";

    var chars = str.split("");
    console.log(chars);
}

main();

